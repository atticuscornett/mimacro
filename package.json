{
  "name": "macro",
  "version": "0.0.1",
  "description": "App that does the macro things, pretty epic.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "@sienci/avrgirl-arduino": "^5.0.3",
    "serialport": "^11.0.0"
  },
  "devDependencies": {
    "@electron/rebuild": "^3.2.13",
    "electron": "^24.3.0"
  }
}
