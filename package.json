{
  "name": "macro",
  "version": "0.0.1",
  "description": "App that does the macro things, pretty epic.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ."
  },
  "author": "",
  "license": "GPL-3.0",
  "dependencies": {
    "serialport": "^11.0.0"
  },
  "devDependencies": {
    "electron": "^24.3.0"
  }
}
